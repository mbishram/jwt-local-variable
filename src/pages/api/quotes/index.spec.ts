import quoteApi from "./index.page";

describe("API Quotes", () => {
	it("should be able to CRUD", () => {
		console.log(quoteApi);
		// Get without input data
		//	Create two post
		//	check if there is two psot
		// Delet post
	});

	it.skip("should throw an error on method other than GET and POST", () => {});
});

export {};
