import { checkAuth } from "@/libs/api/check-auth";

describe("Check Auth", () => {
	describe("when JWT doesn't exist", () => {
		it.todo("should return error");
	});

	describe("when JWT is valid", () => {
		it.todo("should return data");
	});

	describe("when JWT is invalid", () => {
		it.todo("should return error");
	});
});
